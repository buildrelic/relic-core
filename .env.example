OPENAI_API_KEY=
GITHUB_TOKEN=            # optional; falls back to `gh auth token`
LINEAR_API_KEY=          # optional; enables Linear ingestion
GRAPHITI_LLM_PROVIDER=openai
ENGRAM_DB_PATH=./data/engram.kuzu
REGISTRY_DB_PATH=./data/registry.db
SEMAPHORE_LIMIT=10
