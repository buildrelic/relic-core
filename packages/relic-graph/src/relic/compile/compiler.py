"""LLM compiler: fill a SkillIR from grounded evidence via structured output.

Every populated field must cite its source; anything ungrounded is dropped, not
invented; status is always `draft` out of the compiler. Filled in Phase 4.
"""
