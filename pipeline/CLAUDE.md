# pipeline: orchestration entry point (Phase 8 placeholder)

Will become the single ingest, resolve, detect, compile orchestrator, likely a Cloud Run Job. Today the pipeline is driven by individual `relic` CLI commands instead.

## Files

- `run.py`: `main()` raises NotImplementedError.

## Maintaining this file

Update it in the same commit as a change that invalidates it. Replace the placeholder notes when orchestration lands. Current state only: history lives in the git log.
