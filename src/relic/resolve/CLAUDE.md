# resolve: cross-source identity resolution (Phase 3 stub)

Will unify people across GitHub and Linear into single `Person` nodes: deterministic key matching first, LLM only for ambiguous cases. Not implemented yet.

## Files

- `people.py`: docstring-only stub.

## Invoked by

`relic resolve` in `cli.py`, which currently raises a not-implemented error.

## Maintaining this file

Update it in the same commit as a change that invalidates it. Replace the stub notes when Phase 3 lands. Current state only: history lives in the git log.
